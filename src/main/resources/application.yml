spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

  jackson:
    serialization:
      INDENT_OUTPUT: true

server:
  port: 8080

logging:
  level:
    root: INFO
    com.edgar.inventory: DEBUG
